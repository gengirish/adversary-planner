# CLI Reference

> Auto-generated by `doc-sync` on 2026-04-01 14:11 UTC.
> Do not edit — re-run the generator to update.

Entry point: `aplanner` (installed via `pip install -e .`)

## Commands

| Command | Description |
|---------|-------------|
| `aplanner new` | Create a new campaign from a target YAML file. |
| `aplanner list` | List all campaigns. |
| `aplanner next` | Get next technique recommendations for a campaign. |
| `aplanner status` | Show detailed campaign status. |
| `aplanner garak` | Import garak JSONL report into a campaign. |
| `aplanner report` | Generate a campaign report. |
| `aplanner techniques` | List all techniques in the catalog. |
| `aplanner families` | List all technique families. |

## Command Details

### `aplanner new`

Create a new campaign from a target YAML file.

**Arguments:**
- `'target_file'`

**Options:**
- `'--name', '-n'`
- `'--adaptive/--static'`
- `'--dir', 'base_dir'`

### `aplanner list`

List all campaigns.

**Options:**
- `'--dir', 'base_dir'`

### `aplanner next`

Get next technique recommendations for a campaign.

**Arguments:**
- `'campaign_id'`

**Options:**
- `'--count', '-c'`
- `'--no-diversify'`
- `'--dir', 'base_dir'`

### `aplanner status`

Show detailed campaign status.

**Arguments:**
- `'campaign_id'`

**Options:**
- `'--dir', 'base_dir'`

### `aplanner garak`

Import garak JSONL report into a campaign.

**Arguments:**
- `'campaign_id'`
- `'report_file'`

**Options:**
- `'--dir', 'base_dir'`

### `aplanner report`

Generate a campaign report.

**Arguments:**
- `'campaign_id'`

**Options:**
- `'--output', '-o'`
- `'--dir', 'base_dir'`

### `aplanner techniques`

List all techniques in the catalog.

**Options:**
- `'--family', '-f'`

### `aplanner families`

List all technique families.

